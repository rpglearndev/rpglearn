# US-020 — E2E

## Veredicto

**PASS**

## Evidencia

```
& Godot_v4.6.3-stable_win64.exe --headless --path client/godot -s res://tests/run_tests.gd
exit 0
All tests passed (TickWorld + ManualTickInput + World01 + MvpData + Lua + Editor + Validator + Combat).
```

## Manual

| Paso | Comprobar |
|------|-----------|
| F5 World_01 + MVP data | HUD: `HP … | XP … | Oro …` |
| Editor Lua, script con `nearestEnemy` + `attack("mob_0")` | Daño y XP/oro al matar slime cercano |
| Clase archer + `setDesiredRange(3)` | Ataca a 3 tiles, no a 4 |
