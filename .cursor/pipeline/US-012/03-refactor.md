# US-012 — Refactor

## Cambios

- Preloads consistentes en layout/factory (sin `class_name` circular).
- `run_tests.gd` valida carga de suites.
- Filtro Nearest documentado vía `project.godot` (no API rota en 4.6).

Comportamiento: sin cambios. Tests: PASS.
